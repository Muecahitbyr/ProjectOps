node_modules
dist
.env
.env.*
!.env.example
!.env.production.example

# Firebase-Service-Account-Schluessel (Rohdateien, nicht base64-kodiert) -
# duerfen nie ins Repo, werden ausschliesslich als env var auf dem Server
# hinterlegt (siehe FIREBASE_SERVICE_ACCOUNT_<PROJECT_ID> in .env.example).
pkapps/
